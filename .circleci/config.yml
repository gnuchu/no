version: 2
jobs:
  build:
    docker:
      - image: ubuntu:16.04
    steps:
      - run: 
          name: Apt Update
          command: '/usr/bin/sudo sudo apt-get update; /usr/bin/sudo sudo apt-get -y install build-essential libtool automake pkg-config git wget gettext-base'
      - run:
          name: Make
          command: make